export enum E_POST_TYPE {
    IMAGE = 'image',
    TEXT = 'text',
    VIDEO = 'video',
  }
  
  export enum E_POST_STATUS {
    DELETED = 'deleted',
    PUBLISHED = 'published',
    SAVED = 'saved',
    SCHEDULED = 'scheduled',
  }